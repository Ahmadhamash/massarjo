# دليل نظام طلبات السيرة الذاتية

## نظرة عامة
تم تفعيل نظام طلبات السيرة الذاتية بالكامل! الآن يمكن للمستخدمين إرسال طلبات السيرة الذاتية ويمكن للأدمن رؤيتها وإدارتها من لوحة التحكم.

---

## ✅ ما تم إنجازه

### 1. **Backend (الخادم)**
- ✅ Model: `src/models/CvRequest.js` - يحتوي على جميع الحقول المطلوبة
- ✅ Routes: `src/routes/cvRequests.js` - يوفر endpoints للإنشاء والعرض والتحديث
- ✅ Server: تم تسجيل المسارات في `server.js`

### 2. **Frontend (الواجهة)**
- ✅ صفحة النموذج: `cv-booking.html` - تحتوي على النموذج الكامل
- ✅ الدوال في `mind.js`:
  - `openCvBookingForPackage()` - لفتح النموذج مع بيانات الباقة
  - `loadAdminCvRequests()` - لعرض الطلبات في لوحة الأدمن
  - `updateCvRequestStatus()` - لتحديث حالة الطلب
  - `getCvRequestStatusText()` - لترجمة الحالات
  - `loadUserCvRequests()` - لعرض طلبات المستخدم

### 3. **لوحة التحكم**
- ✅ إضافة زر "طلبات السيرة الذاتية" في القائمة الجانبية
- ✅ قسم عرض الطلبات: `admin-cv-requests`
- ✅ إمكانية تغيير حالة الطلب من لوحة الأدمن

### 4. **الملفات والمجلدات**
- ✅ مجلد `uploads/cvs/` لتخزين الملفات المرفوعة
- ✅ ملف `.gitkeep` للحفاظ على المجلد في Git

---

## 🚀 كيفية استخدام النظام

### للمستخدمين:

1. **زيارة صفحة السيرة الذاتية:**
   - افتح `cv-booking.html` أو من الصفحة الرئيسية

2. **اختيار الباقة:**
   - انقر على زر "احجز" في أي باقة
   - سيتم فتح نموذج الطلب مع بيانات الباقة تلقائياً

3. **ملء النموذج:**
   - الاسم الكامل
   - البريد الإلكتروني
   - رقم الهاتف
   - المستوى الوظيفي الحالي
   - المسمى الوظيفي المستهدف
   - سنوات الخبرة
   - ملف LinkedIn (اختياري)
   - رفع السيرة الذاتية الحالية (اختياري)
   - ملاحظات (اختياري)

4. **إرسال الطلب:**
   - انقر على "إرسال الطلب"
   - ستظهر رسالة نجاح

### للأدمن:

1. **تسجيل الدخول:**
   - سجل الدخول كأدمن من الصفحة الرئيسية

2. **الوصول لطلبات السيرة الذاتية:**
   - في لوحة التحكم، انقر على "طلبات السيرة الذاتية" من القائمة الجانبية

3. **إدارة الطلبات:**
   - يمكنك رؤية جميع الطلبات
   - تغيير حالة الطلب (قيد الانتظار / قيد المراجعة / مكتمل / ملغي)
   - تحميل ملف السيرة الذاتية المرفوع
   - رؤية جميع التفاصيل:
     * اسم المستخدم ورقم الهاتف
     * اسم الباقة والسعر
     * المستوى الوظيفي
     * المسمى المستهدف
     * سنوات الخبرة
     * ملف LinkedIn
     * الملاحظات

---

## 📊 حالات الطلب

- **pending** (قيد الانتظار): طلب جديد لم يتم مراجعته بعد
- **in_progress** (قيد المراجعة): جاري العمل على الطلب
- **completed** (مكتمل): تم الانتهاء من الطلب
- **cancelled** (ملغي): تم إلغاء الطلب

---

## 🔧 التشغيل

### تشغيل السيرفر:

```bash
cd "C:\Users\ahmad hamash\OneDrive\سطح المكتب\html\massarfinal"
npm start
```

أو

```bash
node server.js
```

### المتطلبات:
- Node.js مثبت
- MongoDB مفعل ومتصل
- جميع الحزم مثبتة (`npm install`)

---

## 🔐 الأمان

- ✅ جميع endpoints محمية بـ Authentication Token
- ✅ يجب تسجيل الدخول قبل إرسال أي طلب
- ✅ فقط الأدمن يمكنه رؤية جميع الطلبات
- ✅ المستخدم العادي يرى طلباته فقط

---

## 📁 الملفات المهمة

```
massarfinal/
├── src/
│   ├── models/
│   │   └── CvRequest.js          # نموذج البيانات
│   ├── routes/
│   │   └── cvRequests.js         # المسارات والـ endpoints
│   └── middleware/
│       ├── auth.js               # التحقق من المستخدم
│       └── adminAuth.js          # التحقق من الأدمن
├── uploads/
│   └── cvs/                      # مجلد تخزين الملفات المرفوعة
├── server.js                     # الخادم الرئيسي
├── mind.js                       # الدوال الخاصة بالواجهة
├── index.html                    # الصفحة الرئيسية + لوحة الأدمن
└── cv-booking.html               # صفحة حجز السيرة الذاتية
```

---

## 🧪 الاختبار

### 1. اختبار إرسال طلب:
1. افتح `http://localhost:5000/cv-booking.html`
2. انقر على أي باقة
3. املأ النموذج
4. أرسل الطلب
5. تأكد من ظهور رسالة النجاح

### 2. اختبار لوحة الأدمن:
1. سجل دخول كأدمن
2. افتح "طلبات السيرة الذاتية"
3. تأكد من ظهور الطلب الذي أرسلته
4. جرب تغيير حالة الطلب
5. تأكد من إمكانية تحميل الملف المرفوع

---

## 🎉 النظام جاهز للاستخدام!

تم تفعيل نظام طلبات السيرة الذاتية بالكامل ويعمل بشكل صحيح.
جميع الميزات المطلوبة متوفرة والنظام محمي بالكامل.

---

**ملاحظات إضافية:**
- تأكد من تشغيل MongoDB قبل بدء السيرفر
- الملفات المرفوعة يتم تخزينها في `uploads/cvs/`
- يمكن للمستخدم رؤية طلباته من قسم "طلباتي" في حسابه
