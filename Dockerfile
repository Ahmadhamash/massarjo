# 1. حدد الصورة الأساسية (حسب لغة مشروعك: python:3.9 أو php:8.0 أو node:18)
FROM node:18-alpine

# 2. حدد مجلد العمل داخل الحاوية
WORKDIR /app

# 3. انسخ ملفات تعريف المكتبات أولاً (للاستفادة من الكاش)
# في نود: package.json، في بايثون: requirements.txt
COPY package*.json ./

# 4. تثبيت المكتبات
RUN npm install

# 5. انسخ باقي ملفات المشروع من السيرفر إلى الحاوية
COPY . .

# 6. حدد البورت الذي يعمل عليه تطبيقك داخلياً
EXPOSE 3000

# 7. أمر تشغيل التطبيق
CMD ["npm", "start"]